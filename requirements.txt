# PyTorch - 已修复 DLL 错误，使用稳定版本
# 如果遇到 DLL 错误，请使用以下命令重新安装：
# pip uninstall torch torchvision torchaudio -y
# pip install torch==2.0.1 torchvision==0.15.2 --index-url https://download.pytorch.org/whl/cpu
torch==2.0.1
torchvision==0.15.2
numpy<2.0
matplotlib>=3.7.0
